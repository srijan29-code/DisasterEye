# Supabase (pre-provisioned — values are already set in the hosted environment)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Gemini API key for real AI analysis (when wiring up the edge function)
# GEMINI_API_KEY=your_gemini_api_key
